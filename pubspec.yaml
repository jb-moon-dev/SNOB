name: snob
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  http: ^1.1.0
  cupertino_icons: ^1.0.8

  # Kakao Login
  kakao_flutter_sdk_user: ^2.0.0+1

  # Kakao Map
  kakao_map_plugin: 0.3.7

  # Secure Token Storage
  flutter_secure_storage: ^9.2.2
  flutter_map: ^8.3.1
  latlong2: ^0.10.1


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0


flutter:
  uses-material-design: true

  assets:
    - assets/map/sigungu_congestion.geojson
    - congestion_final.csv # 또는 위치한 경로